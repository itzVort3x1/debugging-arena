# Database — SQLite for local dev (relative to prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# NextAuth
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""

# Anthropic
ANTHROPIC_API_KEY=""
