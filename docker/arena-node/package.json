{
  "name": "arena-node-runtime",
  "private": true,
  "description": "Pinned test toolchain baked into the arena-node image. Versions track the app's own devDependencies so in-container runs match host runs.",
  "dependencies": {
    "jest": "30.4.2",
    "ts-jest": "29.4.9",
    "ts-node": "10.9.2",
    "typescript": "5.9.3"
  }
}
